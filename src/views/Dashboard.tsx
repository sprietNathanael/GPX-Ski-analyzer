import { Container } from '@mui/material';

function DashboardPage() {
	return <Container maxWidth='xl' sx={{ flexGrow: 1, paddingBottom: '10px', overflow: 'hidden' }}></Container>;
}

export default DashboardPage;
